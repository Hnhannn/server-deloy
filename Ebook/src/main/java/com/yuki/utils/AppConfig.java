package com.yuki.utils;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // @Bean
    // public NumberFormat currencyFormatter() {
    // return NumberFormat.getCurrencyInstance(new Locale("vi", "VN")); // Định dạng
    // tiền tệ Việt Nam
    // }
}